#include "bits/stdc++.h"
using namespace std;

int closestsum(vector<int> arr,int n,int x){
    sort(arr.begin(),arr.end());
    vector<int> a;
    for(int i=0;i<n;i++){
        if(a.size()<3){
            a.push_back(arr[i]);
        }
        else{
            
        }
    }

}

signed main(){
    return 0;
}